extends layout
block title
    .jumbotron
        table(style='width:100%')
            tr
                td(style='width:50%')
                    h1 LieToMe!
                    p Lie to your friends. 
                td(align='right')
                    button#serverButton(type='button' action='' class='btn btn-default' style="font-size:150%") server
                td(align='right')
                    button#clientButton(type='button' class='btn btn-default' style="font-size:150%") klijent
     
    script.
        $('#serverButton').click(function(evt){
            window.location = 'server';
            return;
        });

        $('#clientButton').click(function(evt){
            window.location = 'home';
            return;
        });